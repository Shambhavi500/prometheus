import { KnowledgeView } from '@/features/knowledge/KnowledgeView';

export const metadata = { title: 'Knowledge — PROMETHEUS' };

export default function KnowledgePage() {
  return <KnowledgeView />;
}
