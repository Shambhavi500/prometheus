import { RepositoryView } from '@/features/repository/RepositoryView';

export const metadata = {
  title: 'Project Repository — PROMETHEUS',
};

export default function RepositoryPage() {
  return <RepositoryView />;
}
