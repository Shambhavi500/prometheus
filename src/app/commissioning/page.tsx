import { CommissioningView } from '@/features/commissioning/CommissioningView';

export const metadata = { title: 'Commissioning — PROMETHEUS' };

export default function CommissioningPage() {
  return <CommissioningView />;
}
